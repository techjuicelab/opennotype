# notype · OpenNoType 비용 비교 — 2026-09-09

## 결론과 확인 범위

동일한 녹음 시간과 청구 토큰 수를 가정하면, notype의 기본 Groq 조합은 현재 OpenNoType 설정보다 API 비용이 약 79.7% 낮다. 하루 30분 사용 예시는 월 **notype $1.09 / OpenNoType $5.35**다. 품질이나 실제 사용 토큰 수가 같다는 의미는 아니다.

- OpenNoType: 커밋 `12910c1`, 이 Mac에 저장된 제공자·모델 설정 확인. OpenAI, 모델 override 없음, 로컬 음성 인식 꺼짐.
- notype: 커밋 `76eaf20`, 저장소 기본 모델과 비용 집계 코드 확인. Mac의 backendURL은 `https://notype.techjuicelab.space`다.
- notype 운영 서버의 환경변수는 확인하지 못했다. NAS 읽기 전용 조회가 SSH 에이전트 서명 실패로 중단됐으므로 아래 notype 모델과 예산은 **배포 실측값이 아닌 저장소 기본값**이다.
- 공식 가격표를 조회했으며 유료 AI 요청, 모델 변경, 배포, 개인 녹음·전사문·키 조회는 하지 않았다. 제공자 청구서와 실제 과거 지출은 확인하지 않았다.

## 비용 구조

| 항목 | notype 기본값 | OpenNoType 현재 설정 |
| --- | --- | --- |
| 음성 인식 | Groq `whisper-large-v3-turbo` | OpenAI `gpt-transcribe` |
| 문장 정리 | Groq `openai/gpt-oss-120b` | OpenAI `gpt-4.1-mini` |
| 장애 시 전환 | 재시도 가능한 Groq 오류에서 해당 단계만 OpenAI로 1회 전환; OpenAI 키 필요 | 다른 제공자로 자동 전환하지 않음 |
| API 비용 부담 | NAS 서버 운영자의 Groq/OpenAI 계정; 여러 사용자 비용이 합산됨 | 사용자가 연결한 API 계정 |
| 비용 기록 | 서버 원장과 관리자 오늘·이번 달 합계 있음; 공개 단가로 계산한 추정액 | 사용량·비용 원장 없음 |
| 별도 운영 비용 | NAS·전력·네트워크 등; 이번 계산 제외 | API 외 앱 실행 기기의 연산·전력; 이번 계산 제외 |

notype의 OpenAI 전환 기본값은 음성 인식 `gpt-4o-mini-transcribe-2025-12-15`, 문장 정리 `gpt-5.6-luna`다. 환경변수로 변경할 수 있다. 실패한 기본 요청과 전환 요청 모두 비용이 발생할 가능성이 있으므로, 기본 조합 가격만으로 장애 시 총액을 단정할 수 없다.

## 공식 단가

| 항목 | notype 기본 조합 | OpenNoType 현재 조합 |
| --- | --- | --- |
| 음성 인식 | 시간당 $0.04, 분당 약 $0.000667 | 분당 $0.0045 |
| 문장 정리 입력 100만 토큰 | $0.15 | $0.40 |
| 문장 정리 출력 100만 토큰 | $0.60 | $1.60 |

근거: [Groq 음성 모델](https://console.groq.com/docs/model/whisper-large-v3-turbo), [Groq 문장 모델](https://console.groq.com/docs/model/openai/gpt-oss-120b), [OpenAI 공식 가격표](https://developers.openai.com/api/docs/pricing). 표준 유료 단가이며 아래 예시는 무료 크레딧, 캐시 할인, 세금을 반영하지 않았다.

Groq 음성 인식은 **요청마다 최소 10초를 청구**한다. 따라서 5초 녹음을 여러 번 하는 경우 단순한 총 녹음 시간 비례 계산보다 비싸다. [Groq 음성 인식 문서](https://console.groq.com/docs/speech-to-text)

## 같은 사용량의 월 비용 예시

월 30일, 녹음 1회 1분, 매회 공통 지침 등을 포함한 전체 입력 2,000토큰·청구 출력 400토큰, 실패·재시도·제공자 전환 없음으로 가정했다. 화면에 보이는 글자 수만으로 출력 토큰을 계산하지 않는다. 추론 등 추가로 청구되는 토큰이 있으면 이 가정에 포함하거나 별도로 더해야 한다.

| 하루 녹음 | 월 녹음 | notype 음성 | notype 문장 | notype 합계 | OpenNoType 합계 |
| --- | --- | --- | --- | --- | --- |
| 10분 | 300분 | $0.20 | $0.162 | **$0.36** | **$1.78** |
| 30분 | 900분 | $0.60 | $0.486 | **$1.09** | **$5.35** |
| 60분 | 1,800분 | $1.20 | $0.972 | **$2.17** | **$10.69** |

notype 문장 정리 1회 = `(2,000 × 0.15 + 400 × 0.60) / 1,000,000 = $0.00054`.
OpenNoType 문장 정리 1회 = `(2,000 × 0.40 + 400 × 1.60) / 1,000,000 = $0.00144`.

두 앱은 지침·문맥·사전·모델이 다르므로 실제 토큰 수와 재시도 비율도 달라질 수 있다. 특히 짧고 잦은 사용에서는 문장 정리 호출 수와 최소 청구 길이가 중요하다. 이 표는 단가 비교를 위한 가정이며 실제 월 청구액 예측에는 사용량 계측이 필요하다.

## notype 비용 집계에서 확인한 오차

| 문제 | 코드와 영향 |
| --- | --- |
| 오래된 Groq 출력 단가 | `server/lib/ai.ts:877`이 $0.75를 사용한다. 현재 공식 $0.60보다 해당 출력 비용을 25% 높게 계산한다. |
| 10초 최소 청구 누락 | `server/lib/ai.ts:908`은 전달받은 실제 길이에 비례해 계산한다. 10초 미만 요청 비용을 낮게 집계한다. |
| OpenAI 전환 단가가 모델과 무관 | `server/lib/ai.ts:883`의 모델별 표가 비어 있고 입력 $2.50·출력 $10/100만 토큰을 공통 적용한다. 기본 `gpt-5.6-luna`의 현재 표준 단가 $0.20/$1.20보다 높다. 운영 모델이 바뀌면 그 모델과 다시 대조해야 한다. |
| 캐시 할인 미반영 | `server/lib/ai.ts:765` 부근에서 전체 입력·출력 토큰만 읽는다. 캐시가 적용된 요청은 할인 전 금액으로 계산한다. |
| 추정액 표시 누락 | 서버 `server/index.ts:1244`가 추정액·단가표 버전을 반환하지만 Swift 응답 모델과 관리 화면에서 안내하지 않는다. `macos/notype/UI/ManagementWindow.swift:1893` 부근의 비용 화면은 청구서로 오해될 수 있다. |

비용 장부가 있다는 점은 장점이지만, 현재 표시액이 제공자 실제 청구액과 같지는 않다. 성공한 요청만 세는 구조는 아니며 실패·전환 시도도 원장에 남긴다. 사용량을 알 수 없는 경우에는 보수적인 예약 추정치가 유지될 수 있다.

notype 서버 기본 예산은 전체 사용자 합계 월 **$1 알림 / $3 경고 / $5 신규 요청 중단**이다(`server/lib/db.ts:66`). 운영 환경변수·관리자 DB 설정으로 바꿀 수 있고 현재 운영 한도는 미확인이다. 서버가 계산한 추정액·예약액에 따른 앱 자체 제어이므로 제공자 청구 한도와 같지 않다.

## 판단

API 단가만 보면 notype가 저렴하다. OpenNoType을 개인적으로 하루 30분 쓰는 위 예시에서는 월 차액이 약 $4.26이므로, 한국어 전사 정확도와 문장 정리 품질을 같은 녹음으로 비교한 뒤 모델을 선택하는 편이 타당하다. 여러 사용자가 쓰는 notype에서는 사용자 수에 따라 운영자가 부담하는 총액이 늘어난다.

비용 관리는 notype의 단가·최소 청구·추정액 표시를 교정하고, OpenNoType에는 음성 길이·실제 응답 모델·입력 및 출력 토큰·실패 상태를 기록하는 것이 우선이다. 이번에는 비교 보고서만 추가했으며 두 앱의 동작과 서버 설정은 변경하지 않았다.

OpenNoType의 상세 호출 구조와 개선 항목은 [OpenNoType 비용 점검](cost-audit-2026-09-09.md)을 참고한다.
